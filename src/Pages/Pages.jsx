import React from 'react';
import PropTypes from 'prop-types';

const Pages = props => (
  <PagesWrapper>
    This is a Pages
  </PagesWrapper>
);

Pages.propTypes = {
  // bla: PropTypes.string,
};

Pages.defaultProps = {
  // bla: 'test',
};

export default Pages;
